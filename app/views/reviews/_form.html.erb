<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, label: 'Content', as: :text %>
  <%= f.input :rating, label: 'Rating (5 as highest)', collection: (0..5)  %>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>
